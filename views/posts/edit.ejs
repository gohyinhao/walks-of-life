<% include ../partials/header %>

<div id="main-content-container">
    <h1 id="header" class="ui header">Edit <%= post.title %></h1>
    <div id="content" class="ui container">
        <form class="ui form" action="/posts/<%= post._id %>?_method=PUT" method="POST">
            <div class="field">
                <label>Title</label>
                <input type="text" name="post[title]" placeholder="Title" value="<%= post.title %>">
            </div>
            <div class="field">
                <label>Content</label>
                <textarea name="post[content]" id="wysiwyg-editor"><%= post.content %></textarea>
            </div>
            <button class="ui green basic button" type="submit">Save!</button>
        </form>

        <p><a class="ui black basic button" href="/posts/<%= post._id %>">Go Back!</a></p>
    </div>
</div>

<% include ../partials/froalaEditor %>
<% include ../partials/footer %>
